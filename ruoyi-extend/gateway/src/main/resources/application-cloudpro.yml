server:
  #  port: 8070
  port: 9000
  servlet:
    context-path: /

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: metrics,refresh,health,info
# 日志配置
logging:
  level:
    org.springframework: warn
    org.mybatis.spring.mapper: error
    org.apache.fury: warn
  config: classpath:logback.xml

spring:
  cloud:
    # 负载均衡器缓存
    loadbalancer:
      cache:
        enabled: false
        caffeine:
          spec: initialCapacity=500,expireAfterWrite=5s
    consul:
      #      host: 192.168.100.17
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: gateway
        healthCheckPath: /actuator/health
        prefer-ip-address: true
        healthCheckInterval: 15s
        #        ip-address: 192.168.100.13
        port: ${server.port}
      enabled: false
    nacos:
      config:
        import-check:
          enabled: false
        server-addr: ${CLOUD_NACOS_CONFIG_SERVER_ADDR:nacos:8848}      # Nacos Server 地址（host:port）
        file-extension: yaml                 # 配置文件类型
        namespace: ${CLOUD_NACOS_CONFIG_SERVER_NAMESPACE:20d01292-0678-4996-a566-228ac9bb8e2c} # 可选：命名空间
        group: DEFAULT_GROUP                 # 可选：分组
        refresh-enabled: true                # 支持热更新

      # Nacos Discovery 服务注册
      discovery:
        server-addr: ${CLOUD_NACOS_CONFIG_SERVER_ADDR:nacos:8848}
        namespace: ${CLOUD_NACOS_CONFIG_SERVER_NAMESPACE:20d01292-0678-4996-a566-228ac9bb8e2c}
        service: ${spring.application.name}
        cluster-name: DEFAULT
#        ip: ${CLOUD_NACOS_CONFIG_CLIENT_IP:127.0.0.1}
        port: ${CLOUD_NACOS_CONFIG_CLIENT_PORT:${server.port}}
        heartbeat-interval: 5000              # 心跳间隔，ms
        heartbeat-timeout: 15000              # 超时，ms
